# Lint / type toolchain, mirroring the house set. Versions pinned so a tool
# release cannot turn CI red on an unchanged commit. The package and suites
# are standard-library only; git-filter-repo is optional for a handful of
# end-to-end rewrite tests and is never installed by the wheel.
pycodestyle==2.14.0
pylint==4.0.6
pyright==1.1.408
